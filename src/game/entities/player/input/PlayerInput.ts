export interface PlayerInput {
    isLeftFired: boolean;
    isRightFired: boolean;
    isDashFired: boolean;
    isJumpFired: boolean;
    isDownFired: boolean;
    isUseFired: boolean;
    isHandFired: boolean;

    moveIntensive: number;
}
